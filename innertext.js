document.getElementById("test").innerText ="Hello!";
// we can use "textcontent" instead of "innertext"
// Note : in innertext tag the html script tag should be after body tag.
console.log(document.getElementById("test").textContent);